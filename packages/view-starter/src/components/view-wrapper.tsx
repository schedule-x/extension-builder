import { PreactViewComponent } from "../create-view";

export const ViewWrapper: PreactViewComponent = ({ $app }) => {
  return (
    <div className="sx__resource-view">
      <h1>View starter</h1>
    </div>
  )
}
